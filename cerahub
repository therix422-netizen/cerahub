local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer

-- BLACK SCREEN
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "CeraHubIntro"
screenGui.Parent = localPlayer:WaitForChild("PlayerGui")
screenGui.ResetOnSpawn = false

local blackFrame = Instance.new("Frame")
blackFrame.Size = UDim2.new(1,0,1,0)
blackFrame.BackgroundColor3 = Color3.new(0,0,0)
blackFrame.Parent = screenGui

-- STAR DROPPING BACKGROUND
local starsFolder = Instance.new("Folder")
starsFolder.Name = "Stars"
starsFolder.Parent = blackFrame

-- Function to spawn a star
local function spawnStar()
    local star = Instance.new("Frame")
    star.Size = UDim2.new(0,2,0,2)
    star.BackgroundColor3 = Color3.fromRGB(255,255,255)
    star.BorderSizePixel = 0
    star.Position = UDim2.new(math.random(),0,0,0)
    star.AnchorPoint = Vector2.new(0.5,0.5)
    star.Parent = starsFolder

    -- Animate falling
    local tween = TweenService:Create(star, TweenInfo.new(3 + math.random(), Enum.EasingStyle.Linear), {Position = UDim2.new(star.Position.X.Scale, 0, 1, 0)})
    tween:Play()
    tween.Completed:Connect(function()
        star:Destroy()
    end)
end

-- Spawn stars continuously
local starConnection
starConnection = RunService.Heartbeat:Connect(function()
    if math.random() < 0.1 then
        spawnStar()
    end
end)

-- TEXT LABELS
local title = Instance.new("TextLabel")
title.Size = UDim2.new(0,300,0,50)
title.Position = UDim2.new(0.5,-150,0.4,0)
title.BackgroundTransparency = 1
title.Text = "Cera Hub"
title.TextColor3 = Color3.fromRGB(255,255,255)
title.Font = Enum.Font.GothamBold
title.TextSize = 36
title.TextTransparency = 1
title.Parent = blackFrame

local credit = Instance.new("TextLabel")
credit.Size = UDim2.new(0,300,0,30)
credit.Position = UDim2.new(0.5,-150,0.5,0)
credit.BackgroundTransparency = 1
credit.Text = "Credit: Synergy"
credit.TextColor3 = Color3.fromRGB(255,255,255)
credit.Font = Enum.Font.Gotham
credit.TextSize = 20
credit.TextTransparency = 1
credit.Parent = blackFrame

-- FADE IN ANIMATION
TweenService:Create(title, TweenInfo.new(1.5), {TextTransparency = 0}):Play()
TweenService:Create(credit, TweenInfo.new(1.5, Enum.EasingStyle.Linear, Enum.EasingDirection.In,0, false,1.5), {TextTransparency = 0}):Play()

-- REMOVE BLACK SCREEN AFTER 3s
delay(3, function()
    starConnection:Disconnect()
    blackFrame:Destroy()
end)

local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local player = Players.LocalPlayer

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "CeraHub"
screenGui.IgnoreGuiInset = true
screenGui.ResetOnSpawn = false -- ✅ Prevent GUI from being destroyed on death
screenGui.Parent = player:WaitForChild("PlayerGui")

-- Main Frame
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 500, 0, 300)
frame.Position = UDim2.new(0.5, -250, 0.5, -150)
frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
frame.BorderSizePixel = 0
frame.AnchorPoint = Vector2.new(0.5, 0.5)
frame.Parent = screenGui
frame.Visible = false
frame.Active = true
frame.Draggable = true

local uicorner = Instance.new("UICorner")
uicorner.CornerRadius = UDim.new(0, 12)
uicorner.Parent = frame

-- Title Bar
local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, 0, 0, 40)
titleBar.BackgroundColor3 = Color3.fromRGB(180, 40, 40)
titleBar.BorderSizePixel = 0
titleBar.Parent = frame

local titleText = Instance.new("TextLabel")
titleText.Size = UDim2.new(1, -40, 1, 0)
titleText.Position = UDim2.new(0, 10, 0, 0)
titleText.BackgroundTransparency = 1
titleText.Text = "CeraHub"
titleText.TextColor3 = Color3.fromRGB(255, 255, 255)
titleText.Font = Enum.Font.GothamBold
titleText.TextSize = 22
titleText.TextXAlignment = Enum.TextXAlignment.Left
titleText.Parent = titleBar

-- Close Button
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 40, 1, 0)
closeBtn.Position = UDim2.new(1, -40, 0, 0)
closeBtn.BackgroundTransparency = 1
closeBtn.Text = "X"
closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextSize = 22
closeBtn.Parent = titleBar

-- Cube (Minimized state)
local cube = Instance.new("TextButton")
cube.Size = UDim2.new(0, 100, 0, 40)
cube.Position = UDim2.new(0.5, -50, 0.5, -20)
cube.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
cube.BorderSizePixel = 0
cube.Text = "CeraHub"
cube.TextColor3 = Color3.fromRGB(255, 255, 255)
cube.Font = Enum.Font.GothamBold
cube.TextSize = 18
cube.Visible = false
cube.Parent = screenGui
cube.Active = true
cube.Draggable = true

local cubeCorner = Instance.new("UICorner")
cubeCorner.CornerRadius = UDim.new(0, 12)
cubeCorner.Parent = cube

-- Tabs Holder
local tabsFrame = Instance.new("Frame")
tabsFrame.Size = UDim2.new(0, 120, 1, -40)
tabsFrame.Position = UDim2.new(0, 0, 0, 40)
tabsFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
tabsFrame.BorderSizePixel = 0
tabsFrame.Parent = frame

local tabLayout = Instance.new("UIListLayout")
tabLayout.SortOrder = Enum.SortOrder.LayoutOrder
tabLayout.Padding = UDim.new(0, 5)
tabLayout.Parent = tabsFrame

-- Content Holder
local contentFrame = Instance.new("Frame")
contentFrame.Size = UDim2.new(1, -120, 1, -40)
contentFrame.Position = UDim2.new(0, 120, 0, 40)
contentFrame.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
contentFrame.BorderSizePixel = 0
contentFrame.Parent = frame

-- Tab system
local currentTab = nil

local function createTab(tabName)
    local tabButton = Instance.new("TextButton")
    tabButton.Size = UDim2.new(1, -10, 0, 30)
    tabButton.BackgroundColor3 = Color3.fromRGB(55, 55, 55)
    tabButton.Text = tabName
    tabButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    tabButton.Font = Enum.Font.GothamBold
    tabButton.TextSize = 16
    tabButton.Parent = tabsFrame

    local tabContent = Instance.new("ScrollingFrame")
    tabContent.Size = UDim2.new(1, -10, 1, -10)
    tabContent.Position = UDim2.new(0, 5, 0, 5)
    tabContent.BackgroundTransparency = 1
    tabContent.CanvasSize = UDim2.new(0, 0, 0, 0)
    tabContent.ScrollBarThickness = 6
    tabContent.Visible = false
    tabContent.Parent = contentFrame

    local contentLayout = Instance.new("UIListLayout")
    contentLayout.SortOrder = Enum.SortOrder.LayoutOrder
    contentLayout.Padding = UDim.new(0, 8)
    contentLayout.Parent = tabContent

    -- Switch tab
    tabButton.MouseButton1Click:Connect(function()
        if currentTab then
            currentTab.Visible = false
        end
        tabContent.Visible = true
        currentTab = tabContent
    end)

    return tabContent
end

local function createButton(parent, btnName, pastebinRaw)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -10, 0, 40)
    btn.BackgroundColor3 = Color3.fromRGB(180, 40, 40)
    btn.Text = btnName
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 18
    btn.Parent = parent

    local uicorner = Instance.new("UICorner")
    uicorner.CornerRadius = UDim.new(0, 8)
    uicorner.Parent = btn

    btn.MouseButton1Click:Connect(function()
        local success, err = pcall(function()
            loadstring(game:HttpGet(pastebinRaw))()
        end)
        if not success then
            warn("Failed to load Pastebin script:", err)
        end
    end)
end

-- Example Usage:
local tab1 = createTab("Usefull")
createButton(tab1, "Infinite Yield", "https://pastebin.com/raw/6rrhy0P1")
createButton(tab1, "Expand All hitboxes", "https://pastebin.com/raw/2vW3rbTU")
createButton(tab1, "Anti Hit(might still get hit a couple times)", "https://pastebin.com/raw/vXX19QqU")
createButton(tab1, "Reload Hub(If version is not updated)", "https://pastebin.com/raw/HCQAsLZE")

local tab2 = createTab("Fun")
createButton(tab2, "Weird Animation(Toggle R)", "https://pastebin.com/raw/DLWc4Vsb")
createButton(tab2, "Auto fight (Toggle F and equip slap/bat)", "https://pastebin.com/raw/rghH1Ukq")

local tab3 = createTab("Farm")
createButton(tab3, "Auto Buy Rarity", "https://pastebin.com/raw/KATc9Dre")
createButton(tab3, "Auto Collect Money", "https://gist.githubusercontent.com/synergypestral-afk/3cb28023788411362b84c69d5a97cea6/raw/e17a70aa583db398f44a62f00e362850552dcbe6/gg")

local tab4 = createTab("Vuln")
createButton(tab4, "Teleport CLones", "https://pastebin.com/raw/ppZC9y1t")
createButton(tab4, "Kill players", "https://pastebin.com/raw/Peu5WiHN")
createButton(tab4, "(OP admin panel)", "https://gist.githubusercontent.com/synergypestral-afk/a73d20bfbb0d61cc167431b97d26b7bf/raw/931a750803db86f4593cc02ccbf2d816c9c25677/ffff%2520admin")

local tab5 = createTab("ESP")
createButton(tab5, "ESP Good Brainrots", "https://gist.githubusercontent.com/synergypestral-afk/25eb2cca3a1013c3b17cb8a05cbce0f1/raw/020beb8ac42a5113438648af8df6900f6073b3d7/esp")
createButton(tab5, "ESP Player", "https://pastebin.com/raw/EEzazcvS")

local tab6 = createTab("Auto tool")
createButton(tab6, "Jail Gui", "https://gist.githubusercontent.com/synergypestral-afk/ea74011dfa3497916dd0e104b5011dd2/raw/4af49b4e179b332eafbfad1c13da00502880420d/gistfile1.txt")
createButton(tab6, "Aimbot(equip lasercape/slingshot toggle B)", "https://pastebin.com/raw/46EJS9LB")
createButton(tab6, "Auto Websling with K (Must be close and holding web)", "https://pastebin.com/raw/jmZSNABX")
createButton(tab6, "Give Player Seizure", "https://gist.githubusercontent.com/synergypestral-afk/822dedfd9290b11ad20268004e2a685c/raw/eed160b0b94f9bfa8b993640e0fc9eefa40f0fd1/fffff")

local tab7 = createTab("Misc")
createButton(tab7, "Float", "https://pastebin.com/raw/waNxPbuY")
createButton(tab7, "Emoji Helper(cool) !", "https://pastebin.com/raw/mujQt5wA")
createButton(tab7, "Noclip Cam", "https://gist.githubusercontent.com/synergypestral-afk/e82f6fab2fb27d4cab3f6288cf14225e/raw/e44ba0c99c32e0338c2b00af3241724d294adf50/camera")

local function openGUI()
    cube.Visible = false
    frame.Size = UDim2.new(0, 0, 0, 0)
    frame.BackgroundTransparency = 1
    frame.Visible = true

    TweenService:Create(frame, TweenInfo.new(0.6, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        Size = UDim2.new(0, 500, 0, 300),
        BackgroundTransparency = 0
    }):Play()
end

local function closeGUI()
    local tween = TweenService:Create(frame, TweenInfo.new(0.5, Enum.EasingStyle.Quint, Enum.EasingDirection.In), {
        Size = UDim2.new(0, 0, 0, 0),
        BackgroundTransparency = 1
    })
    tween:Play()
    tween.Completed:Connect(function()
        frame.Visible = false
        cube.Visible = true
    end)
end

closeBtn.MouseButton1Click:Connect(closeGUI)
cube.MouseButton1Click:Connect(openGUI)

-- Start open
openGUI()
